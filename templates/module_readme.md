# Module <ID>: <Title>

## Overview
- What this module covers and why it matters.

## Learning Goals
- Bullet the outcomes learners should achieve.

## Prereqs & Setup
- Required keys/env vars (do not commit secrets).
- Install: `python -m venv venv && source venv/bin/activate && pip install -r requirements.txt`

## Run
- Primary entrypoint: `python deliverable_<topic>.py [args]`
- Demo/quickstart: `python deliverable_<topic>.py demo`
- Expected artifacts/logs and where they land.

## Repro Notes
- Seed(s) set and logged.
- Data/model hashes and config saved (see `../docs/repro.template.yaml`).
- Safety checks or evals performed.
